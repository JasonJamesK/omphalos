---
phase: 3
slug: markdown-editing-session-prep-fields
status: draft
nyquist_compliant: true
wave_0_complete: true
created: 2026-07-13
---

# Phase 3 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | None currently configured for the frontend — unchanged from Phase 2's finding. `package.json` has no `test` script, no `vitest`/`jest` devDependency, no `*.test.jsx` files anywhere in `src/client/`. |
| **Config file** | none — no framework needed for this phase |
| **Quick run command** | n/a — conversational UAT (`npm run dev`, manual smoke check) |
| **Full suite command** | n/a — conversational UAT via `/gsd-verify-work` |
| **Estimated runtime** | n/a |

This phase is 100% frontend UI wiring — consistent with Phase 1 and Phase 2's closure pattern, this project validates such changes through conversational UAT, not automated frontend tests.

---

## Sampling Rate

- **After every task commit:** Manual smoke check in the browser (`npm run dev`) — type sample markdown into the field just wired, confirm preview renders, save, reload, confirm persistence.
- **After every plan wave:** Re-run all 4 field locations plus the Loot key-stability manual test.
- **Before `/gsd-verify-work`:** Full conversational UAT covering all 3 phase success criteria.
- **Max feedback latency:** immediate (manual browser check, no test suite to wait on)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 03-01-* | 03-01 | TBD | MDED-01 | — | react-markdown default rendering, no `dangerouslySetInnerHTML`/`rehype-raw` | manual (visual + reload) | n/a — UAT | ❌ Wave 0 (framework absent) | ⬜ pending |
| 03-01-* | 03-01 | TBD | MDED-02 | — | Same as above; applies to Notes/Callout/Loot | manual (visual + reload) | n/a — UAT | ❌ Wave 0 (framework absent) | ⬜ pending |
| 03-01-* | 03-01 | TBD | MDED-09 | — | N/A | code-review-verifiable | `grep -rn "MarkdownField" src/client/components/tabs/SessionPrep.jsx src/client/components/session/blocks/` | N/A — verifiable via grep, no test file needed | ⬜ pending |
| 03-01-* | 03-01 | TBD | Loot key-stability fix (correctness fix surfaced by research, not a formal REQ) | — | N/A | manual (visual): set item 2's field to Preview, delete item 1, confirm item 2's (now item 1's) tab state is correct | n/a — UAT | ❌ Wave 0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

*Task IDs finalized once PLAN.md is generated by the planner — this table's rows map 1:1 to the Phase Requirements → Test Map in `03-RESEARCH.md`.*

---

## Wave 0 Requirements

*None — no frontend test framework exists, and this phase's scope (component wiring + one correctness fix) does not independently justify introducing one, consistent with Phase 2's precedent.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Overview & Hook markdown write/preview/save/reload | MDED-01 | No frontend test framework in this project; conversational UAT is the established pattern (Phase 1, Phase 2) | Type markdown into Overview & Hook, confirm live preview, save, reload page, confirm both raw markdown and rendered preview persist |
| Notes/Callout/Loot markdown write/preview/save/reload | MDED-02 | Same as above | Type markdown into each of Notes, Callout, and Loot description fields, confirm live preview, save, reload, confirm persistence for each |
| Loot item list key-stability | Correctness fix (research-surfaced) | No frontend test framework; regression is a UI state bug only observable by interacting with the list | Add 2+ loot items, set one item's `MarkdownField` to Preview tab, delete a different item above it, confirm the surviving item's tab state didn't scramble |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies — N/A (no test framework in project); manual UAT is the automated-equivalent per project convention
- [x] Sampling continuity: no 3 consecutive tasks without automated verify — N/A, all verification is manual UAT by design
- [x] Wave 0 covers all MISSING references — none required
- [x] No watch-mode flags
- [x] Feedback latency < immediate (manual smoke check)
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
