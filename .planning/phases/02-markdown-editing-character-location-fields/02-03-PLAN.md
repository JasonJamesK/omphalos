---
phase: 02-markdown-editing-character-location-fields
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/client/components/tabs/Locations.jsx
  - src/client/components/location/AddLocationModal.jsx
  - src/client/components/character/AddFromLibraryModal.jsx
autonomous: true
requirements: [MDED-03, MDED-04]
must_haves:
  truths:
    - "A DM editing an in-session location (EditLocationModal) writes Session Notes in markdown, and the read-only Description in the Shared Info box renders as markdown (D-04, D-05, MDED-04)"
    - "A DM creating/linking a location (AddLocationModal) writes Description, Secrets & Hazards, Notes, and Session Notes in markdown; the confirm-step read-only Description renders markdown (D-03, D-05, MDED-04)"
    - "A DM linking a library character (AddFromLibraryModal) writes Session Notes in markdown; the character preview fields show clean plain text (D-01, D-13, MDED-03)"
    - "Read-only Secrets & Hazards displays keep their ▸-per-line bullet rendering, unchanged (D-12); the location card's untruncated Session Notes renders full markdown (D-14)"
  artifacts:
    - "src/client/components/tabs/Locations.jsx (EditLocationModal + LocationCard wired)"
    - "src/client/components/location/AddLocationModal.jsx (create + notes steps wired)"
    - "src/client/components/character/AddFromLibraryModal.jsx (CharacterPreview + notes step wired)"
  key_links:
    - "EditLocationModal read-only Description → MarkdownPreview; Session Notes → MarkdownField"
    - "LocationCard truncated Description → stripMarkdown; untruncated Session Notes → MarkdownPreview (D-14)"
    - "AddFromLibraryModal CharacterPreview fields → stripMarkdown; Session Notes → MarkdownField"
---

<objective>
Roll the proven markdown components (built in Plan 02-01) out to the remaining in-session location and character-link sites: the session Locations tab (`Locations.jsx`), the add/create-location flow (`AddLocationModal.jsx`), and the add-character-from-library flow (`AddFromLibraryModal.jsx`). After this plan every editable D-01/D-03 field across the app writes markdown, every named read-only "Shared Info" Description renders markdown (D-05), hazard bullets are preserved (D-12), card snippets are strip-cleaned (D-13), and the location card's Session Notes renders full markdown (D-14).

This closes the location half (MDED-04) and the character-link session-notes path (MDED-03). `LocationsLibrary.jsx` is EXCLUDED (D-04 — dead code).

Purpose: Complete the in-session location editing/preview surface and the library-link session-notes surface using the single shared component (MDED-09 groundwork).
Output: Three modified in-session components with markdown editing, read-only markdown Shared Info, preserved hazard bullets, and strip-cleaned previews.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-markdown-editing-character-location-fields/02-CONTEXT.md
@.planning/phases/02-markdown-editing-character-location-fields/02-PATTERNS.md
@.planning/phases/02-markdown-editing-character-location-fields/02-UI-SPEC.md
@.planning/phases/02-markdown-editing-character-location-fields/02-VALIDATION.md
@.planning/phases/02-markdown-editing-character-location-fields/02-01-PLAN.md
</context>

## Phase Goal

**As a** DM, **I want to** write character bios/notes and location descriptions using markdown syntax with a live preview, **so that** my prep reads cleanly with headings, lists, and emphasis in the app's dark theme instead of as flat run-on text.

*(This plan extends the Plan 02-01 components to the in-session location + library-link flows.)*

## MVP Slice

Each task adds a real capability: after Task 1 a DM can use markdown in the session location edit modal; after Task 2, in the add/create-location flow; after Task 3, in the add-character-from-library flow. Verification is the manual UAT smoke check from `02-VALIDATION.md` (manual-UAT-only; `npm run build` is the automated regression guard). Depends on Plan 02-01 for `MarkdownField`, `MarkdownPreview`, `stripMarkdown`.

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Wire markdown into the session Locations tab (EditLocationModal + LocationCard)</name>
  <files>src/client/components/tabs/Locations.jsx</files>
  <read_first>
    - src/client/components/tabs/Locations.jsx (full file — EditLocationModal read-only Description line 36, Secrets/Hazards custom bullet renderer lines 37-44, Session Notes textarea lines 48-55; LocationCard truncated description line 111-115, hazard bullet list lines 116-128, Session Notes display lines 129-134; `inputCls` line 6)
    - src/client/components/markdown/MarkdownField.jsx, MarkdownPreview.jsx, stripMarkdown.js (created in Plan 02-01)
    - .planning/phases/02-markdown-editing-character-location-fields/02-CONTEXT.md (D-04 in-session location scope, D-05 read-only Description renders markdown, D-12 keep hazard bullets, D-13 strip card snippets, D-14 LocationCard Session Notes gets full MarkdownPreview)
    - .planning/phases/02-markdown-editing-character-location-fields/02-PATTERNS.md (Locations.jsx assignment lines 150-162)
  </read_first>
  <action>
    Add imports at the top of `Locations.jsx`: `MarkdownField`, `MarkdownPreview` from `../markdown/...`, and `stripMarkdown` from `../markdown/stripMarkdown`.

    In `EditLocationModal`:
    - Replace the read-only Description display (line 36, currently `{loc.description && <p className="text-sm text-[#d4d4d4] leading-relaxed">{loc.description}</p>}`) with `<MarkdownPreview value={loc.description} className="text-sm text-[#d4d4d4]" />` (D-05 — read-only Shared Info Description renders markdown).
    - Replace the Session Notes plain `<textarea>` (lines 48-55) with `<MarkdownField value={sessionNotes} onChange={setSessionNotes} className={inputCls} />` (D-04 — SessionNotes editable in scope). Note the local state setter here is `setSessionNotes` (string), so `onChange={setSessionNotes}` works directly.
    - Leave the Secrets & Hazards custom `▸`-per-line bullet renderer (lines 37-44) UNCHANGED (D-12 — do not run through markdown or strip).

    In `LocationCard`:
    - Wrap the truncated Description (line 113) so `stripMarkdown(loc.description)` runs before the 220-char truncation (D-13); render the stripped/truncated string in the same `<div>`. Do NOT use `MarkdownPreview` for this truncated snippet.
    - Leave the `hazardLines` `▸` bullet list (lines 116-128) UNCHANGED (D-12).
    - Replace the untruncated Session Notes display (line 132, currently `<p ...>{loc.sessionNotes}</p>`) with `<MarkdownPreview value={loc.sessionNotes} className="text-xs text-[#d4d4d4]" />` (D-14 — this is a full, untruncated read-only display of a markdown-authored field, so it gets full markdown rendering, NOT stripMarkdown).

    Do NOT embed planning/decision/requirement identifiers in source comments (project CLAUDE.md).
  </action>
  <verify>
    <automated>npm run build</automated>
    <human-check>In `npm run dev`, open a session's Locations tab. Edit a library-linked location: the Shared Info Description renders markdown (`**bold**` shows bold, not asterisks); Session Notes is a MarkdownField; the Secrets/Hazards box still shows `▸`-per-line bullets. Add markdown Session Notes, save. On the location card: the Description snippet is clean plain text (no `**`/`#`), the hazard `▸` bullets are unchanged, and the Session Notes area renders full markdown (D-14).</human-check>
  </verify>
  <acceptance_criteria>
    - `Locations.jsx` imports `MarkdownField`, `MarkdownPreview`, `stripMarkdown`.
    - `EditLocationModal`: read-only Description is a `MarkdownPreview`; Session Notes is a `MarkdownField`; the Secrets/Hazards `▸` bullet renderer is unchanged.
    - `LocationCard`: truncated Description runs through `stripMarkdown`; hazard bullets unchanged; Session Notes renders through `MarkdownPreview` (D-14).
    - `npm run build` exits 0; live check confirms markdown Shared Info, preserved hazard bullets, clean card Description, and full-markdown card Session Notes.
    - No planning/requirement identifiers in source comments.
  </acceptance_criteria>
  <done>The session Locations tab edits Session Notes in markdown, renders read-only Description and card Session Notes as markdown, preserves hazard bullets, and strips card Description snippets.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire markdown into the add/create-location flow (AddLocationModal)</name>
  <files>src/client/components/location/AddLocationModal.jsx</files>
  <read_first>
    - src/client/components/location/AddLocationModal.jsx (full file — create-step Description line 209, Secrets & Hazards line 213, Notes line 217; notes-step read-only Description line 238, Secrets/Hazards custom bullet renderer lines 239-246, Session Notes textarea lines 250-257; `inputCls` line 14)
    - src/client/components/markdown/MarkdownField.jsx, MarkdownPreview.jsx (created in Plan 02-01)
    - .planning/phases/02-markdown-editing-character-location-fields/02-CONTEXT.md (D-03 location field scope, D-04 in-session scope, D-05 read-only Description renders markdown, D-12 keep hazard bullets)
    - .planning/phases/02-markdown-editing-character-location-fields/02-PATTERNS.md (AddLocationModal assignment lines 136-146)
  </read_first>
  <action>
    Add imports at the top of `AddLocationModal.jsx`: `MarkdownField`, `MarkdownPreview` from `../markdown/...`.

    In the create step, replace the plain `<textarea>` for these three fields with `<MarkdownField value={createForm.<field>} onChange={v => setCreateForm(p => ({ ...p, <field>: v }))} className={inputCls} />` (D-03, D-04, MDED-04):
    - Description (line 209) → `description`
    - Secrets & Hazards (line 213) → `secretsAndHazards` (editable form field IS a MarkdownField per D-03)
    - Notes (line 217) → `notes`

    In the notes step:
    - Replace the read-only Description display (line 238) with `<MarkdownPreview value={selected.description} className="text-sm text-[#d4d4d4]" />` (D-05).
    - Leave the Secrets & Hazards custom `▸`-per-line bullet renderer (lines 239-246) UNCHANGED (D-12).
    - Replace the Session Notes plain `<textarea>` (lines 250-257) with `<MarkdownField value={sessionNotes} onChange={setSessionNotes} className={inputCls} />` (SessionNotes editable in scope).

    Keep the multi-step navigation, image upload, `handleCreate`, and `handleConfirm` logic unchanged. Do NOT embed planning/decision/requirement identifiers in source comments.
  </action>
  <verify>
    <automated>npm run build</automated>
    <human-check>In `npm run dev`, open a session's Locations tab → Add Location → Create new shared location. Type markdown into Description, Secrets & Hazards, Notes — each shows live dark-themed preview + auto-grow (side-by-side at ~540px width). Proceed to the notes step: the read-only Description renders markdown, Secrets/Hazards still shows `▸` bullets, Session Notes is a MarkdownField. Confirm add-to-session works.</human-check>
  </verify>
  <acceptance_criteria>
    - `AddLocationModal.jsx` imports `MarkdownField` and `MarkdownPreview`.
    - Create-step `description`, `secretsAndHazards`, `notes` are `MarkdownField` writing through `setCreateForm`.
    - Notes-step read-only Description is `MarkdownPreview`; Session Notes is `MarkdownField`; the Secrets/Hazards `▸` bullet renderer is unchanged.
    - `npm run build` exits 0; live check confirms all create-step markdown fields, read-only markdown Description, preserved hazard bullets, markdown Session Notes.
    - No planning/requirement identifiers in source comments.
  </acceptance_criteria>
  <done>The add/create-location flow edits all location prose fields in markdown, renders the confirm-step Description as markdown, and preserves hazard bullets.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Wire markdown into the add-character-from-library flow (AddFromLibraryModal)</name>
  <files>src/client/components/character/AddFromLibraryModal.jsx</files>
  <read_first>
    - src/client/components/character/AddFromLibraryModal.jsx (full file — CharacterPreview field rows lines 55-65 rendering personalityTraits/flaw/description/questHooks with `whitespace-pre-wrap`; compact + full call sites lines 150, 174; Session Notes textarea lines 177-186; `inp` alias line 5)
    - src/client/components/markdown/MarkdownField.jsx, stripMarkdown.js (created in Plan 02-01)
    - .planning/phases/02-markdown-editing-character-location-fields/02-CONTEXT.md (D-01 SessionNotes editable scope, D-13 strip-markdown for CharacterPreview read-only fields)
    - .planning/phases/02-markdown-editing-character-location-fields/02-PATTERNS.md (AddFromLibraryModal assignment lines 166-172)
  </read_first>
  <action>
    Add imports at the top of `AddFromLibraryModal.jsx`: `MarkdownField` from `../markdown/MarkdownField`, `stripMarkdown` from `../markdown/stripMarkdown`.

    In `CharacterPreview`, the read-only field rows (lines 55-65) map over `[['Personality', char.personalityTraits], ['Flaw', char.flaw], ['Description', char.description], ['Quest Hooks', char.questHooks]]` and render each `value` as raw text. Wrap each rendered value with `stripMarkdown(value)` so markdown syntax characters do not clutter this compact read-only preview (D-13 — this is a pre-add preview outside D-05's named Shared Info scope, so strip, NOT full render). Keep the existing per-field label + `whitespace-pre-wrap` paragraph styling; only the value string is passed through `stripMarkdown`. Do NOT use `MarkdownPreview` here and do NOT use the `remove-markdown` package.

    In the notes step, replace the Session Notes plain `<textarea>` (lines 177-186) with `<MarkdownField value={sessionNotes} onChange={setSessionNotes} className={inp} />` (D-01 — SessionNotes editable in scope). Keep the `handleAdd`, step navigation, and character-list logic unchanged.

    Do NOT embed planning/decision/requirement identifiers in source comments.
  </action>
  <verify>
    <automated>npm run build</automated>
    <human-check>In `npm run dev`, open a session's Characters tab → Add from Library. Select a character whose Description contains `**bold**` and "C# is great": the preview shows clean plain text (no literal `**`/`#`) with `C#` preserved. Proceed to the Session Notes step — it is now a MarkdownField with live preview; add markdown notes and confirm add-to-session works. Repeat with the compact preview shown in the notes step.</human-check>
  </verify>
  <acceptance_criteria>
    - `AddFromLibraryModal.jsx` imports `MarkdownField` and `stripMarkdown`.
    - `CharacterPreview` passes `personalityTraits`, `flaw`, `description`, `questHooks` values through `stripMarkdown` before rendering; it does NOT use `MarkdownPreview` or `remove-markdown`.
    - The notes-step Session Notes field is a `MarkdownField`.
    - `npm run build` exits 0; live check confirms clean stripped preview (incl. preserved `C#`) and markdown Session Notes.
    - No planning/requirement identifiers in source comments.
  </acceptance_criteria>
  <done>The add-character-from-library flow shows clean stripped character previews and edits Session Notes in markdown.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DM-authored in-session text → rendered preview / stripped snippet | Same trusted-admin-authored markdown as Plan 02-01; no new endpoint, no schema change, no new trust boundary — only rendering of existing string fields changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering / Information Disclosure | MarkdownField/MarkdownPreview in EditLocationModal, AddLocationModal, LocationCard | medium | mitigate | Reuses the Plan 02-01 MarkdownPreview control — react-markdown default rendering, no `dangerouslySetInnerHTML`, no `rehype-raw`. No override introduced here. |
| T-02-03 | Information Disclosure | stripMarkdown card/preview snippets | low | accept | AST-based strip renders only plain text into card/preview snippets; no HTML rendered in the strip path. No new install; no additional control needed. |
</threat_model>

<verification>
- `npm run build` exits 0 after all three tasks.
- Manual UAT per 02-VALIDATION.md: in-session location edit + add flows render live markdown; read-only Descriptions render markdown (D-05); hazard bullets preserved (D-12); card Description strip-cleaned (D-13); card Session Notes full markdown (D-14); library-character-link Session Notes markdown + stripped preview.
- No new dependency added (consumes Plan 02-01 artifacts only); no new trust boundary.
</verification>

<success_criteria>
- DM writes markdown in all in-session location prose fields and all Session Notes fields with live preview (MDED-03, MDED-04, D-03, D-04, D-01).
- Read-only Description Shared Info renders markdown (D-05); hazard bullets preserved (D-12); card Description snippets clean (D-13); card Session Notes full markdown (D-14).
- `LocationsLibrary.jsx` untouched (D-04 exclusion).
</success_criteria>

## Artifacts this phase produces (this plan)

**Modified:**
- `src/client/components/tabs/Locations.jsx` — EditLocationModal (Description → MarkdownPreview, Session Notes → MarkdownField), LocationCard (Description → stripMarkdown, Session Notes → MarkdownPreview)
- `src/client/components/location/AddLocationModal.jsx` — create-step Description/Secrets&Hazards/Notes → MarkdownField; notes-step Description → MarkdownPreview, Session Notes → MarkdownField
- `src/client/components/character/AddFromLibraryModal.jsx` — CharacterPreview fields → stripMarkdown; Session Notes → MarkdownField

**Consumes (from Plan 02-01):** `MarkdownField`, `MarkdownPreview`, `stripMarkdown`

**No new files, no new npm dependencies, no new CSS selectors.**

<output>
Create `.planning/phases/02-markdown-editing-character-location-fields/02-03-SUMMARY.md` when done.
</output>
