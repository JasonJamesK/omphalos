---
phase: 02-markdown-editing-character-location-fields
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/client/components/Library.jsx
autonomous: true
requirements: [MDED-03, MDED-04]
must_haves:
  truths:
    - "A DM editing a shared library Character (GlobalCharacterModal) writes markdown in Personality Traits, Flaw, Description, and Quest Hooks with a live preview (D-01, D-02, MDED-03)"
    - "A DM editing a shared library Location (GlobalLocationModal) writes markdown in Description, Secrets & Hazards, and Notes with a live preview (D-03, D-04, MDED-04)"
    - "Global character and location cards show clean plain-text description snippets — no literal ** or # clutter, mid-sentence # preserved (D-13)"
  artifacts:
    - "src/client/components/Library.jsx (GlobalCharacterModal + GlobalLocationModal forms wired to MarkdownField; both card previews wired to stripMarkdown)"
  key_links:
    - "GlobalCharacterModal/GlobalLocationModal set(k,v)/setForm helpers drive MarkdownField value/onChange"
    - "GlobalCharacterCard/GlobalLocationCard truncated descriptions run through stripMarkdown before .slice()"
---

<objective>
Roll the proven markdown components (built in Plan 02-01) out to the shared Library — the second half of the Character slice and the first Location slice. After this plan a DM can write markdown in every editable field of the global Character and Location library forms and see clean plain-text snippets in the library grid cards.

This extends D-02 (both in-session AND library for characters) and D-04 (both library AND in-session for locations) to the actual routed library UI: `Library.jsx`'s internal `GlobalCharacterModal` and `GlobalLocationModal` forms and their grid cards. `LocationsLibrary.jsx` is explicitly EXCLUDED (D-04 correction — confirmed dead code, not imported anywhere).

Purpose: Deliver markdown editing for library character prose (MDED-03) and library location fields (MDED-04), reusing the single shared component (MDED-09 groundwork).
Output: `Library.jsx` with both internal forms markdown-enabled and both card previews strip-markdown-cleaned.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-markdown-editing-character-location-fields/02-CONTEXT.md
@.planning/phases/02-markdown-editing-character-location-fields/02-PATTERNS.md
@.planning/phases/02-markdown-editing-character-location-fields/02-UI-SPEC.md
@.planning/phases/02-markdown-editing-character-location-fields/02-VALIDATION.md
@.planning/phases/02-markdown-editing-character-location-fields/02-01-PLAN.md
</context>

## Phase Goal

**As a** DM, **I want to** write character bios/notes and location descriptions using markdown syntax with a live preview, **so that** my prep reads cleanly with headings, lists, and emphasis in the app's dark theme instead of as flat run-on text.

*(This plan extends the Plan 02-01 components to the shared Library forms and grid cards.)*

## MVP Slice

Each task adds a real user-facing capability: after Task 1 a DM can use markdown in the library character form; after Task 2, in the library location form. Verification is the manual UAT smoke check from `02-VALIDATION.md` (manual-UAT-only phase; `npm run build` is the automated regression guard). Depends on Plan 02-01 for `MarkdownField` and `stripMarkdown`.

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Markdown-enable the library Character form + clean its card preview</name>
  <files>src/client/components/Library.jsx</files>
  <read_first>
    - src/client/components/Library.jsx (GlobalCharacterModal `set(k,v)` line 285; character prose textareas: Personality Traits line 343, Flaw line 347, Description line 373, Quest Hooks line 377; GlobalCharacterCard truncated description line 433; shared `inputCls` line 9)
    - src/client/components/markdown/MarkdownField.jsx and stripMarkdown.js (created in Plan 02-01 — the artifacts consumed here)
    - .planning/phases/02-markdown-editing-character-location-fields/02-CONTEXT.md (D-01 field scope, D-02 both-forms scope, D-13 strip-markdown for card previews)
    - .planning/phases/02-markdown-editing-character-location-fields/02-PATTERNS.md (Library.jsx assignment lines 122-133)
  </read_first>
  <action>
    Add imports at the top of `Library.jsx`: `MarkdownField` from `./markdown/MarkdownField` and `stripMarkdown` from `./markdown/stripMarkdown`.

    In `GlobalCharacterModal`, replace the plain `<textarea>` for these four prose fields with `<MarkdownField value={form.<field>} onChange={v => set('<field>', v)} className={inputCls} />` (D-01, D-02, MDED-03):
    - Personality Traits (line 343) → `personalityTraits`
    - Flaw (line 347) → `flaw`
    - Description (line 373) → `description`
    - Quest Hooks (line 377) → `questHooks`
    Keep the labels, two-column grid, portrait, relationships, and save logic unchanged. The existing `set(k,v)` helper (line 285) needs no change since `onChange` yields a raw string.

    In `GlobalCharacterCard` (line 431-435), wrap the truncated description so markdown syntax never leaks into the snippet (D-13): compute `stripMarkdown(char.description)` first, then apply the existing 160-char truncation to the stripped result. Render the stripped/truncated text (still as the same `<div className="px-3 pb-3 text-xs text-[#d4d4d4] ...">`). Do NOT render it through `MarkdownPreview` (card previews use strip, not full render — D-13). Do NOT use the `remove-markdown` package.

    Do NOT embed planning/decision/requirement identifiers in source comments (project CLAUDE.md).
  </action>
  <verify>
    <automated>npm run build</automated>
    <human-check>In `npm run dev`, open Library → Characters → New Character (or Edit). Type markdown into Personality Traits, Flaw, Description, Quest Hooks — each shows a live dark-themed preview and auto-grows; at the ~330px two-column width these show the Edit/Preview tab toggle. Save; confirm the grid card's description snippet shows clean plain text (no literal `**`/`#`), and a description containing "C# is great" keeps the `#`.</human-check>
  </verify>
  <acceptance_criteria>
    - `Library.jsx` imports `MarkdownField` and `stripMarkdown`.
    - `GlobalCharacterModal` renders `personalityTraits`, `flaw`, `description`, `questHooks` as `MarkdownField` with `onChange={v => set(...)}`.
    - `GlobalCharacterCard` passes `char.description` through `stripMarkdown` before truncation; it does NOT use `MarkdownPreview` or `remove-markdown`.
    - `npm run build` exits 0; live check confirms preview render + clean card snippet incl. preserved `C#`.
    - No planning/requirement identifiers in source comments.
  </acceptance_criteria>
  <done>The shared-library character form edits markdown and its card shows clean plain-text snippets.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Markdown-enable the library Location form + clean its card preview</name>
  <files>src/client/components/Library.jsx</files>
  <read_first>
    - src/client/components/Library.jsx (GlobalLocationModal `setForm` inline setters; location textareas: Description line 87, Secrets & Hazards line 91, Notes line 95; GlobalLocationCard truncated description line 140, hazard bullet list lines 143-155; shared `inputCls` line 9)
    - src/client/components/markdown/MarkdownField.jsx and stripMarkdown.js (created in Plan 02-01)
    - .planning/phases/02-markdown-editing-character-location-fields/02-CONTEXT.md (D-03 location field scope, D-04 both-forms scope, D-12 keep hazard bullet rendering read-only, D-13 strip-markdown for card previews)
    - .planning/phases/02-markdown-editing-character-location-fields/02-PATTERNS.md (Library.jsx location assignment lines 128, 130-132)
  </read_first>
  <action>
    In `GlobalLocationModal`, replace the plain `<textarea>` for these three fields with `<MarkdownField value={form.<field>} onChange={v => setForm(p => ({ ...p, <field>: v }))} className={inputCls} />` (D-03, D-04, MDED-04):
    - Description (line 87) → `description`
    - Secrets & Hazards (line 91) → `secretsAndHazards` (this is the EDITABLE form field, which IS a MarkdownField per D-03; its READ-ONLY display elsewhere keeps custom bullets per D-12 — those are different sites, handled in Plan 02-03)
    - Notes (line 95) → `notes`
    Keep the image upload, name/type inputs, and save logic unchanged.

    In `GlobalLocationCard` (line 138-142), run `stripMarkdown(loc.description)` before the existing 200-char truncation so the description snippet is clean plain text (D-13). Leave the `hazardLines` bullet list (lines 143-155) UNCHANGED — the read-only per-line `▸` hazard rendering is kept as-is (D-12; do not run it through markdown or strip). Do NOT use the `remove-markdown` package.

    Do NOT embed planning/decision/requirement identifiers in source comments.
  </action>
  <verify>
    <automated>npm run build</automated>
    <human-check>In `npm run dev`, open Library → Locations → New Location (or Edit). Type markdown into Description, Secrets & Hazards, Notes — each shows a live dark-themed preview and auto-grows; at the ~540px single-column width these show side-by-side edit+preview. Save; confirm the grid card's description snippet is clean plain text (no literal `**`/`#`) and the Secrets/Hazards bullet list still renders one `▸` bullet per line (unchanged, D-12).</human-check>
  </verify>
  <acceptance_criteria>
    - `GlobalLocationModal` renders `description`, `secretsAndHazards`, `notes` as `MarkdownField` with `onChange` writing through `setForm`.
    - `GlobalLocationCard` passes `loc.description` through `stripMarkdown` before truncation; the `hazardLines` `▸` bullet list is unchanged.
    - `npm run build` exits 0; live check confirms side-by-side split at ~540px, clean card snippet, and preserved hazard bullets.
    - No planning/requirement identifiers in source comments.
  </acceptance_criteria>
  <done>The shared-library location form edits markdown, its card description is clean, and the hazard bullet rendering is preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DM-authored library text → rendered preview / stripped snippet | Same trusted-admin-authored markdown as Plan 02-01; no new endpoint, no new trust boundary — this plan only changes rendering of existing string fields |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering / Information Disclosure | MarkdownField preview (via shared MarkdownPreview) in library forms | medium | mitigate | Reuses the Plan 02-01 MarkdownPreview control — react-markdown default rendering, no `dangerouslySetInnerHTML`, no `rehype-raw`. No override introduced here. |
| T-02-03 | Information Disclosure | stripMarkdown card snippets | low | accept | AST-based strip renders only plain text into card previews; no HTML rendered at all in the card path. No new install; no additional control needed. |
</threat_model>

<verification>
- `npm run build` exits 0 after both tasks.
- Manual UAT per 02-VALIDATION.md: library character + location forms render live markdown, cards show clean stripped snippets, hazard bullets preserved (D-12).
- No new dependency added (consumes Plan 02-01 artifacts only); no new trust boundary.
</verification>

<success_criteria>
- DM writes markdown in all four library character prose fields and all three library location fields with live preview (MDED-03, MDED-04, D-01, D-03).
- Both library grid cards show clean plain-text description snippets with mid-sentence `#` preserved (D-13).
- Library location card hazard bullets remain unchanged (D-12).
</success_criteria>

## Artifacts this phase produces (this plan)

**Modified:** `src/client/components/Library.jsx`
- `GlobalCharacterModal` — `personalityTraits`, `flaw`, `description`, `questHooks` now `MarkdownField`
- `GlobalLocationModal` — `description`, `secretsAndHazards`, `notes` now `MarkdownField`
- `GlobalCharacterCard`, `GlobalLocationCard` — description snippet now cleaned via `stripMarkdown`

**Consumes (from Plan 02-01):** `MarkdownField`, `stripMarkdown`

**No new files, no new npm dependencies, no new CSS selectors.**

<output>
Create `.planning/phases/02-markdown-editing-character-location-fields/02-02-SUMMARY.md` when done.
</output>
